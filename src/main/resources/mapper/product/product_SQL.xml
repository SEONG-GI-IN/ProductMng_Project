<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dev.ProductMng.product.dao.ProductDAO">
    <!-- 상품 정보 저장 -->
    <insert id="insertProduct" parameterType="java.util.Map" useGeneratedKeys="true" keyProperty="BAR_CODE">
        INSERT INTO T_PRODUCT_DTL (
            PRODUCT_NAME,
            PRODUCT_PRICE,
            PRODUCT_TYPE_NM,
            PRODUCT_TYPE_CD,
            SUPPLIER,
            PURCHASE_PRICE,
            UPDATE_DT,
            CREATE_DT
        ) VALUES (
             #{productName},
             #{productPrice},
             'TEST',
             'TEST',
             #{supplier},
             #{purchasePrice},
             NOW(),
             NOW()
        )
    </insert>

    <select id="getProductList" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
            BAR_CODE,
            PRODUCT_NAME,
            PRODUCT_PRICE,
            PRODUCT_TYPE_NM,
            PRODUCT_TYPE_CD,
            SUPPLIER,
            PURCHASE_PRICE,
            UPDATE_DT,
            CREATE_DT
        FROM T_PRODUCT_DTL
        ORDER BY CREATE_DT DESC
    </select>
</mapper>